<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SVDAgency" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/brand_red</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/brand_red_dark</item>
        <item name="colorOnPrimaryContainer">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/brand_red</item>
        <item name="colorOnSecondary">@color/white</item>

        <item name="colorSurface">@color/card_background</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="colorSurfaceVariant">@color/card_background</item>
        <item name="colorOnSurfaceVariant">@color/black</item>

        <item name="colorSurfaceContainer">@color/white</item>
        <item name="colorSurfaceContainerHigh">@color/white</item>
        <item name="colorSurfaceContainerHighest">@color/white</item>
        <item name="colorSurfaceContainerLow">@color/white</item>
        <item name="colorSurfaceContainerLowest">@color/white</item>

        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/brand_red</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Window Background -->
        <item name="android:windowBackground">@color/window_background</item>

        <!-- Material Card Styling -->
        <item name="materialCardViewStyle">@style/Widget.App.CardView</item>

        <!-- Navigation View Styling -->
        <item name="navigationViewStyle">@style/Widget.App.NavigationView</item>
    </style>
</resources>